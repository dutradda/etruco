#include <string>
#include <vector>
#include "truco_paulista_rules.h"

void mao_ferro::execute( vector<void*>& v )
{
	/*
	rule.set_state( "mao_ferro", false );
	if( teams_points_state["team1_points"] = 11 and teams_points_state["team2_points"] = 11 ) 
		rule.set_state( "mao_ferro", true );
	*/
}

int mao_ferro::send_my_message( vector<void*>& v )
{
	/*
	int* a = (int*) v.at(0);
	Dealer* dealer = (Dealer*) v.back();
	int* who = (int*) v.at(v.size()-2);
	dealer->send(*who, 1, a, sizeof(int));
	*/
}
